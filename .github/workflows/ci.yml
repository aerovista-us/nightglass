name: ci
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --check server.mjs
      - run: node --check src/app.mjs
      - run: node --check src/db.mjs
      - run: node --check src/engines/registry.mjs
      - run: node --check src/engines/shadowbroker/client.mjs
      - run: node --check src/normalize/finding.mjs
      - run: node --check src/orchestration/trace.mjs
      - run: node --check src/records/sec-company.mjs
      - run: node --check src/store/findings.mjs
      - run: node --check public/app.js
      - run: npm test
      - run: npm run smoke

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          tags: aerovista/nightglass:ci
